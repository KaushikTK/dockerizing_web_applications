# EXPRESS SERVER AND MONGO DB

To build and run, do:
`docker compose up`

To connect to the mongodb, use the name of the container, i.e., 'mongo' in the url instead of localhost.

All the data in the database is saved to data folder for persistent storage.